// @budget/core — framework-agnostic pure logic (no React / DB / DOM).
// Real modules (money, shares, list, ledger, comparison, trends, netBalance)
// are built TDD starting in Phase 1. This barrel grows as they land.

export const CORE_VERSION = '0.0.0';
